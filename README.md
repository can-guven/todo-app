# Install the dependencies
yarn install
# storybook
yarn run storybook
# Start the app in development mode (hot-code reloading, error reporting, etc.)
yarn run start
# Lint the files
yarn run lint
# Format the files
yarn run format
# Run Tests 
yarn run test
